name: scrap_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # state-managment
  bloc: ^8.1.2
  flutter_bloc: ^8.1.5
  bloc_concurrency: ^0.2.2

  #for equilty operator
  freezed_annotation: ^2.4.1
  equatable: ^2.0.5

  #Dependecy injection
  get_it: ^7.6.4

  #routing
  go_router: ^13.0.1

  #local storage
  shared_preferences: ^2.2.2

  #network
  dio: ^5.3.4

  #ui
  device_preview: ^1.1.0

  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
flutter:
  uses-material-design: true

  assets:
    - assets/jsons/
  